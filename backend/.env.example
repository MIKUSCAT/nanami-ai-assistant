# 主模型（用于主要对话）
MAIN_PROVIDER='provider'
MAIN_MODEL='model name'
MAIN_API_KEY='your_api_key_here'
MAIN_BASE_URL='eg：https://api.openai.com/v1'
MAIN_CONTEXT_LENGTH='128000'

# 压缩模型（用于上下文压缩与摘要，节省成本）
COMPACT_PROVIDER='provider'
COMPACT_MODEL='model name'
COMPACT_API_KEY='your_api_key_here'
COMPACT_BASE_URL='eg：https://api.openai.com/v1'
COMPACT_CONTEXT_LENGTH='128000'

# 快速模型（用于简单任务）
QUICK_PROVIDER='provider'
QUICK_MODEL='model name'
QUICK_API_KEY='your_api_key_here'
QUICK_BASE_URL='eg：https://api.openai.com/v1'
QUICK_CONTEXT_LENGTH='128000'

# ==========================================
# SubAgent模型配置（每个SubAgent独立配置）
# ==========================================

# 深度搜索SubAgent（SearchSubAgent）
SEARCH_AGENT_PROVIDER='provider'
SEARCH_AGENT_MODEL='model name'
SEARCH_AGENT_API_KEY='your_api_key_here'
SEARCH_AGENT_BASE_URL='eg：https://api.openai.com/v1'
SEARCH_AGENT_CONTEXT_LENGTH='128000'

# 浏览器SubAgent（BrowserSubAgent）
BROWSER_AGENT_PROVIDER='provider'
BROWSER_AGENT_MODEL='model name'
BROWSER_AGENT_API_KEY='your_api_key_here'
BROWSER_AGENT_BASE_URL='eg：https://api.openai.com/v1'
BROWSER_AGENT_CONTEXT_LENGTH='128000'

# Windows SubAgent（WindowsSubAgent）
WINDOWS_AGENT_PROVIDER='provider'
WINDOWS_AGENT_MODEL='model name'
WINDOWS_AGENT_API_KEY='your_api_key_here'
WINDOWS_AGENT_BASE_URL='eg：https://api.openai.com/v1'
WINDOWS_AGENT_CONTEXT_LENGTH='128000'

# Tavily 搜索
TAVILY_API_KEY='your_tavily_key_here'

# 工具与安全
WORKSPACE_ROOT='E:\七海-agent'
ALLOW_FILE_READ=true


# 服务器设置
HOST=0.0.0.0
PORT='7878'

# 上下文压缩
# 当上下文 token 使用超过该比例时触发自动压缩（0~1 之间）
AUTO_COMPACT_RATIO='0.92'

# 长期记忆（LTM）配置
# 是否启用长期记忆提炼与持久化（1 启用，0 关闭）
LTM_MD_ENABLED=1
# 长期记忆存储路径（Markdown 格式）
LTM_MD_PATH='data/ltm.md'

# 工具结果截断阈值（字节，避免上下文爆炸）
TOOL_RESULT_MAX_SIZE=10240

# ==========================================
# API 速率限制与超时配置（避免502错误）
# ==========================================

# LLM API 最小请求间隔（秒）
# 用于避免高频调用导致API限流（502 Bad Gateway）
# 计算方式：如果rpm=30，则 60/30=2秒，建议设置为2.5秒（留缓冲）
# 默认值：0（不限制，保持向后兼容）
LLM_MIN_INTERVAL=2.5

# 工具执行超时时间（秒）
# SearchSubAgent等复杂任务可能需要较长时间（10-15轮迭代）
# 默认值：120秒可能太短，建议600秒（10分钟）
TOOL_EXECUTION_TIMEOUT=600

# SubAgent 迭代延迟（秒，可选）
# 在 SubAgent 每轮迭代结束后增加延迟，进一步降低API调用频率
# 默认值：0（不延迟）
# 建议值：1.0-2.0秒（配合 LLM_MIN_INTERVAL 使用）
SUBAGENT_ITERATION_DELAY=0
